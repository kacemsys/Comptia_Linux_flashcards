A Linux administrator tries to ping a hostname in a network but receives the message: hostname
not . The administrator is sure the name exists on the network and wants to verify if the DNS
server is found
correctly configured on the client. Which of the following files should the administrator use to
verify this information?
A. /etc/named.conf
B. /etc/resolv.conf
C. /etc/nsswitch.conf
D. /etc/network
